# Engram · Phase 0 verification only. NOT the runtime dependency set.
# Runtime deps land in requirements.txt during Phase 1.
psycopg[binary]>=3.2        # CLAUDE.md §2 pins psycopg3 as the hot path
boto3>=1.35.0               # S3 only (engram-agent-artifacts) — Bedrock is off every code path, CLAUDE.md §2.1
mcp>=1.9.0,<2.0.0           # managed MCP server client — 2.0.0 renamed streamablehttp_client (breaking)
httpx>=0.27.0               # Ollama Cloud client (LLD §6: direct /api/chat, no SDK)
python-dotenv>=1.0.0        # .env loading for the verify scripts
