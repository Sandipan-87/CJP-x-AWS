# Engram · runtime dependencies. scripts/requirements-verify.txt is separate
# (Phase 0 probes only). This file grows as each LLD module lands — nothing
# speculative added ahead of code that actually imports it.
psycopg[binary]>=3.2        # CLAUDE.md §2, agent/memory/db.py — the async pool driver
psycopg_pool>=3.2           # AsyncConnectionPool, used directly by agent/memory/db.py
httpx>=0.27.0               # agent/providers/cohere_embed.py — thin client, no cohere SDK
langgraph>=1.0.0            # agent/graph.py — StateGraph assembly (CLAUDE.md §2)
pydantic>=2.0.0              # agent/schemas.py — Proposal validation (was only a transitive dep before)
